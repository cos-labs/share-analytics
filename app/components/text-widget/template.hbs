<div class="widget-body text-widget {{if item.showButton 'summary'}}">
    <div>{{{item.content}}}
        {{#if item.showButton}}
            <button class="btn btn-info pull-right" {{action "transitionToFacet"}}>Read more</button>
        {{/if}}
    </div>
</div>
