import tsinterval from 'analytics-dashboard/utils/tsinterval';
import { module, test } from 'qunit';

module('Unit | Utility | tsinterval');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tsinterval();
  assert.ok(result);
});
